# SocratesCode AI Evaluator Dependencies

# API clients (optional - only needed for API evaluation)
google-generativeai>=0.3.0
openai>=1.0.0

# Core dependencies (minimal - for local evaluation)
# No external dependencies required for basic functionality!

# Optional: for enhanced features
# sentence-transformers>=2.2.0  # For semantic similarity (if needed later)
